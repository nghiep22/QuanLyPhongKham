# In native development, localhost is replaced with the host running Expo.
# Override this with a reachable HTTPS API URL for preview and production builds.
EXPO_PUBLIC_API_BASE_URL=http://localhost:5000/api/v1
